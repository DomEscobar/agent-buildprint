# Foundation: pinned scaffold and safe packet state

**Compatibility gate:** npm `agent-buildprint@0.1.0` is not published, and public `0.0.17` lacks v2. Use one fresh current-source checkout's matching CLI and local packet, record its HEAD, and keep it unchanged for the run. If that compatible source CLI is unavailable, continue authorized framework implementation and use the upstream framework workflow; report only CLI automation as blocked. Do not invent receipts or relax acceptance. No prebuilt framework archive is supplied.

Complete after alignment and before loop implementation. Read `references/framework.md` and `references/cli-integration.md`. Inspect actual git status and instructions for both checkout and destination; preserve tracked, untracked and staged changes. Never reset, clean, stash or overwrite someone else's work to make setup easier.

1. Resolve all applicable hard-stop decisions from `00-goal.md`. Keep one approved `PROJECT_CONTRACT.md` in the intended host destination. For scaffolding, if the destination has any other files, the scaffold refuses it: use a new sibling host with the approved contract, or stop for the owner's migration decision. Do not delete `.buildprint` or force the scaffold.
2. Use a separate clean framework checkout at the exact commit in `framework-lock.json`. Verify `git rev-parse HEAD`, origin and worktree state. Never checkout over dirty/shared work; clone into an unused directory instead if necessary. Build tools and framework versions come from its actual `package.json` and lockfile, not invented registry releases.
3. Prefer the implemented `agb bootstrap` pinned-template adapter when a trusted prebuilt archive is already available: use the exact command in `README.md`, explicit `--allow-scaffold`, pinned checkout and separately verified `--archive-sha256`. It executes no framework or manifest script and performs no dependency installation. Its archive digest is not proof of build-from-source; missing trustworthy provenance blocks that adapter only. For the upstream preparation path, follow upstream `docs/CREATE_GAME.md`: in that pinned framework checkout run `npm ci`, `npm run build:package`, then `node scripts/create-game.mjs <destination>` with the actual destination. Node >=22.18.0 is required. `build:package` includes boundary/type checks; run these routine prerequisites when implementation is authorized unless an explicit restriction prohibits them.
4. If the adapter already created versioned state, inspect `agb state status` / `agb loop next`; do not start over. Otherwise, in the newly scaffolded host run the local `agb start` sequence documented in `README.md` when the compatible CLI is available. The scaffold preserves the approved contract and vendors `isometric-framework-0.1.0.tgz`; inspect and record its actual SHA-256 and the source commit in `.buildprint/setup-receipt.md`. Do not trust an old tarball merely because its version is unchanged. Follow the generated host README: `npm install`, `npm run dev`, `npm run check`, and `npm run build` as routine authorized implementation verification unless explicitly restricted. Keep host package and lockfile under project ownership.
5. Reuse the installed `node_modules/isometric-framework/skills/README.md` and selected pinned skills. For Buildprint-local harness support, inspect existing `AGENTS.md` and `.agents/skills` first. The supported command is `agb harness init . --provider agents`; it writes reusable skill files. Only run that writer where explicitly permitted by the execution environment. In OpenClaw, creating/modifying reusable skill files must use `skill_workshop`; prefer existing files and linked upstream skills, and record a harness capability blocker rather than manually fabricating them. Never initialize globally. `agb harness check . --provider agents` and `agb harness checkup . --provider agents` are future authorized checks, not commands executed here.
6. Use existing `setup-runbook`, `frontend-ui-product-design`, `subagent-driven-implementation` (only clean independent ownership), and `verify-and-review` where available under their triggers. Run their applicable routine verification with authorized implementation unless explicitly restricted. Preserve the scaffold's AGENTS.md and one-contract rules; no global skill installation is required for framework skills.
7. Write `docs/architecture.md` derived from the contract: host/framework boundary, actual exports, scene/asset authority, input/UI ownership, save lifecycle, dependencies, selected skill paths and gaps. Link `proven_implementation_requirements` from `blueprint.yaml`. Write a secret-free `.env.example` describing no runtime secrets by default, and `.buildprint/setup-receipt.md` using its template. Link contract decisions from `.buildprint/decisions.md` after bootstrap. Technical docs must not become new artistic briefs.

No placeholder commands, real secrets, or fake harness receipts. Do not hide hard-stop blockers. Finish with a reproducible host location, exact package provenance, documented capability/authorization limits and the next eligible production check. Setup is not playable or visual acceptance.

## Versioned routing handoff
After the actual prerequisites exist, record applicable real decisions with `agb state approve` and `agb/receipt/v2` JSON receipts (not invented approvals). `runtime.json` names required approvals and bound files. `agb loop begin` checks prerequisites; `evidence bind/record` and `loop accept/advance` are explicit operations. Open `loop return` to setup/alignment/identity when blocked; preserve history and fix the cause. Consult `references/cli-integration.md` and the matching source checkout's CLI documentation. CLI claims remain attestations; packet setup never authorizes paid providers, skill writes, uploads or deployment. Routine implementation verification follows the approved contract unless it explicitly restricts it.

## Source and evidence hygiene
Record maintained source/test roots, protected input roots and evidence/archive roots in setup proof. Hash exclusion is NOT test-discovery exclusion. Preserve historical source as non-executable text or archives; when authorized, configure the host's actual runner to discover maintained tests and exclude evidence without dropping active fixtures. The pinned scaffold has tsc/Vite, not an existing Vitest runner: do not invent a universal config. When a runner is introduced, test discovery with an archived failing test snapshot (must not run) AND an active failing test (must still fail).
