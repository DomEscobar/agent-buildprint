# Actual running-build capture receipt

Attach this as supporting evidence to the existing framework review; it does not replace production tickets, candidate snapshots or generated submissions. Populate only observed facts.

- Contract requirement IDs and upstream baseline/candidate/ticket:
- Capture time; build command/output identity; actual served local route:
- Source commit plus dirty/untracked inputs; config/lock/runtime identities:
- Loaded scene/map/asset-manifest/atlas hashes and source-to-served-bundle correspondence:
- Actual viewport CSS pixels, device pixel ratio, browser/version, device or emulation, renderer:
- Camera/zoom/floor, UI/game state, normal input sequence and starting conditions:
- Original target/reference role; current capture file and actual SHA-256:
- Full-cycle motion file, wrap/pause/join coverage and observed playback notes:
- Authoritative gameplay outcome and restart/save/mode coverage:
- Any instrumentation, acceleration or deviation from normal play (not hidden):
- Capturer and actual reviewer/session identity; independent/self/unavailable:
- Open defects, unsupported views and capture/source mismatch if any:

No mockups, edited or copied stale screenshots, demo content, arbitrary probe state or frame counters as gameplay/motion proof. Bind fresh captures after every affected repair. A receipt cannot authenticate pixels or reviewer independence by itself.

## Evidence modes and input trace
Record controlled state/time parity and natural trusted-input full-viewport motion separately, bound to the SAME candidate/dist/assets. Parity may reproduce an identical defect; overlays are not unedited runtime proof.
- Mode, source/dist/candidate identity, unedited full viewport and controlled/annotated derivative links:
- Delivered event timestamps, first rendered response, previous/current/interpolated positions, camera transform, native/CSS displacement and chosen metric:
- Tap/hold/chord/release/queued-edge cases, sampling phase, blocked versus open floor, focus/cancel/resize outcomes:
- Decoded clip frame index/time, captured cadence and actual inspected windows; correct labels from viewed frames:
- Legal route and collision trace; behind → emerge → front sequence, object height and expected bounded concealment:
- Menu open/closed, portrait/landscape, camera motion and transformed picking against agreed footprint/scale:

Low furniture may allow only partial concealment: state observed versus inferred limits, never force deep occlusion or walk through solids. Foreground overlap alone is not rear concealment. A cropped canvas does not prove offscreen full-page controls; 4fps samples cannot prove unseen 60fps continuity. These are review prompts, not an automatic host latency/legal-motion checker.
