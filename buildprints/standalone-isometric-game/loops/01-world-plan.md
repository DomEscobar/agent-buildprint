# Protect the whole world and prove the foundation

## How to implement this loop
Read the host `AGENTS.md`, approved `PROJECT_CONTRACT.md`, packet `02-identity.md` and `loops/loop-flow.md`. When CLI state exists, also read `.buildprint/next-agent.md` and resolve packet paths relative to immutable snapshots; otherwise read this packet directly and use upstream framework acceptance receipts. Host data paths are relative to the game root. Load only relevant pinned framework skills under the available tool policy. Run only authorized work, with concise progress updates and durable upstream receipts.

1. Read the pinned new-game guide and visual-loop skill/references in `references/framework.md`. Confirm the scaffold and archive match the recorded pin and contract. Route unsupported features to a real integration decision, not fictional fields.
2. Resolve missing concept direction and rough layout under the approved contract and any generation budget. Supplied or authored direction is equally valid. Derive the complete upstream v3 plan through `templates/acceptance-plan-routing.md`, protecting selected target images and every requirement/view with genuine review mode. Freeze this acceptance baseline before calibration implementation/full-pack production; do not recreate it per candidate.
3. Establish actual projection, chosen pixel scale and a selected original/rights-cleared asset loading in both dev and production. If generated art is needed, satisfy `references/assets.md` first. Eligible implementation may precede a preflight ticket; begin that ticket before collecting its capture/check evidence.
4. Build one semantic authority for host regions/instances/routes/supports. Generate host placement/collision from it. Use public `project` or a level-aware host export, not a generic isometric formula. Confirm spawn, distant approach and crossing landmarks align with the runtime before using any guide as generation input.
5. Use upstream layout scope/checks for applicable topology; do not flatten promised multi-floor links or remove channels to pass the flat checker. Supplement with actual supported host checks/views when authorized. Preserve every protected ID and record unresolved framework/API limitations.
6. Finish eligible preflight/layout checks using actual observations and upstream draft/finish/status; never auto-pass a generated submission. If evidence execution is prohibited, the stage remains unverified and dependent expansion is not falsely authorized.

## Building objective
Translate the approved full game into a supported host architecture, semantic world layout and protected acceptance plan without inventing another brief. Establish every required region, route, landmark, actor action, mode, UI state and device view before detailed asset production. Identify uncertain public API behavior and asset/capture capability early enough to avoid commissioning unusable artwork. A reference supplies style unless layout use was explicitly requested; the original composition and maps belong to this game. The result is a reproducible foundation and eligible preflight/layout receipts, not a static scene advertised as gameplay. Preserve the remaining complete scope even if the riskiest technical experiment is small.

## DO NOT
Use placeholders, functionless buttons or mocked/sample data as final product or live proof. Do not copy demo/example assets/maps, edit framework internals, invent APIs or commands, spend without approval, weaken protected criteria, fake independent review, or reinterpret calibration as the full deliverable. During authorized game work, run the routine build, test and review evidence this loop needs unless the approved contract explicitly restricts it.

## Minimum proof before moving on
Approved single contract and complete plan mapping; original source/reference roles and exact package identity; actual preflight loading and layout/camera evidence under authorized execution. Metadata, rendering and navigation claims remain separate. Unavailable checks/captures are explicit blockers rather than invented receipts.

## Handoff note
Record the actual implemented scope, observed evidence, exact candidate/inputs, open defects and capability/authorization blockers. Link upstream production status/receipts instead of copying their verdicts. When CLI state exists, keep current loop/next eligible work there; otherwise record the next eligible upstream check in the handover. Preserve approvals, full scope and delegated-work ownership.
