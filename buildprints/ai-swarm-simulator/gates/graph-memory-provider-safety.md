# Gate — Graph Memory and Provider Safety

Pass only if:

- Required Zep Cloud dependency is absent.
- Open-source graph memory adapter exists, defaulting to Graphiti or documented OSS alternative.
- Provider config is OpenAI-compatible and runtime-selectable.
- API keys are not committed, logged, or exported.
- Missing provider shows honest blocked state.
