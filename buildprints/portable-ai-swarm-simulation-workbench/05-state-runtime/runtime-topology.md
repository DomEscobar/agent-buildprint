# Runtime Model

Async operations include ontology generation, graph building, simulation preparation, simulation runtime, graph-memory updates, and report generation.

Runtime behavior must expose progress, stale state recovery, stop/force-restart semantics, partial platform failure, process cleanup, and action-log parsing. Subprocess or worker code requires security review before qualification.
