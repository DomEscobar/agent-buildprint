# State Model

Persist project metadata, uploaded file metadata, extracted text, ontology, graph id, task ids, simulation state, profiles, simulation config, run state, action logs, report metadata, report markdown, report progress, report logs, and chat context where applicable.

Durable lifecycle qualification requires restart/readback proof for each state family and safe cleanup behavior for deletion/reset.
