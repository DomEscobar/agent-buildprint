# Phase 6: History And Data Lifecycle

Capability packet: `03-capabilities/06-data-lifecycle/`

Exit only after `proof-contract.yaml` passes or a blocker is recorded in `.buildprint/evidence/evidence-ledger.jsonl`.
