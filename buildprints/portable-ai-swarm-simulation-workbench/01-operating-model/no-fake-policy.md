# No-Fake Policy

Qualification fails if the implementation uses route-shaped endpoints, static UI shells, no-op run/stop/chat/delete buttons, static graph/report data, deterministic adapters counted as live provider proof, or in-memory-only persistence counted as durable lifecycle behavior.
