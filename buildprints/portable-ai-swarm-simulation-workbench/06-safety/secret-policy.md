# Secret Policy

Record environment variable names only: `LLM_API_KEY`, `LLM_BASE_URL`, `LLM_MODEL_NAME`, `ZEP_API_KEY`, and runtime-specific variables. Do not record values. Proof artifacts, logs, screenshots, reports, and ledgers must be scanned for secrets before publication or qualification.
