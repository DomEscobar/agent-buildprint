# One production art and motion family

## How to implement this loop
Read `loops/loop-flow.md`, `.buildprint/next-agent.md`, host `AGENTS.md`, `02-identity.md`, the agreement and current proof. Missing runtime context is a blocker, not permission to guess. Work on the smallest real path and inspect it before expanding.

## Building objective
Read `references/animation.md`, `references/providers-and-sources.md` and `templates/asset-request.md`. Confirm the Asset-Maker preflight evidence for **WaveSpeed + (RetroDiffusion OR Media4Agents)**, including both essential WaveSpeed models, or record the explicit quality limitation and agreed free-only fallback. Custom SVG remains last resort, never equivalent quality; do not silently lower the art target. Start from a rights-cleared seed approved at game scale, then obtain a whole action strip from the same reference only after cost approval when generation is used. Use a shared scale and feet anchor; preserve intentional jump displacement, weapon arcs and anticipation rather than flattening each frame to its opaque box. Declare frame count, rows/columns, source rects, facing, per-frame timing and active hit windows. Keep seed/source identity locked across edits. Normalize transparency by alpha/matte treatment, not deleting black artwork. Load the candidate through the real manifest and shared validation, review contact sheet, motion preview and in-game recording. Approve one movement and one genre action family before scaling content; retain the previous approved version for rollback. Tiles must pass repeated preview and adjacent actor-scale review, not only a single attractive tile.

## DO NOT
Do not pass placeholders as finished art, ship functionless buttons, count mocked/sample data as live proof, or substitute a proof-only runtime for production. Do not expand scope, spend, upload references or change asset rights without agreement. Do not advance on a page-loaded screenshot.

## Minimum proof before moving on
Actual image decode, dimensions, provenance/hash and all source rectangles validated; contact sheet and animation recording at native scale; source identity and shared anchor inspected in engine after movement in supported facings. User approves seed and motion; fallback assets pass these same visual gates. Record verified access or disclosed quality limitations, fallback rights and approval before any paid usage. No paid request performed merely to test this packet.

## Handoff note
Record changed runtime/manifest paths, decisions, commands and observed results, evidence version, remaining blockers, active loop and next smallest repair. State loop_core_passed separately from claim_qualified; use `review.md` before claiming qualification.
