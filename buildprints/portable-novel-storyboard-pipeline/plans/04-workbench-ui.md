# Workbench UI

Use the active `03-phases/*.md` workbench phase. UI proof must show storyboard frames, timeline lanes, selected-shot inspector, task state, blocked-provider state, and export affordance in a real browser when claimed.
