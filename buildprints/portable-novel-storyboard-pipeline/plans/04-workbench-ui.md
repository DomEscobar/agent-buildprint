# 04 Workbench UI

This compatibility phase exists for `agb analyze --phase 04-workbench-ui`.

Use `03-capabilities/production-storyboard-flow/` and `03-capabilities/media-preview-export/` for the current executable packet. UI proof must show storyboard frames, timeline lanes, selected-shot inspector, task state, blocked-provider state, and export affordance in a real browser.

