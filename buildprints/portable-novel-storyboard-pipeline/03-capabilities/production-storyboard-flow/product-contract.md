# Product Contract: production-storyboard-flow

The implementation must provide a workbench that displays and persists director plan, storyboard table, storyboard panel rows, selected-shot details, and flow state.

The UI must not degrade to generic dashboard cards. It must show storyboard-oriented artifacts as the primary surface.

