autonomy_levels:
  workflow:
    description: bounded deterministic implementation sequence
    allowed_decisions:
      - internal module names
      - local helper structure
    forbidden_decisions:
      - dropping source-backed obligations
      - claiming proof without evidence-ledger rows

