# Executable Packet Good Fixture

This file is a compatibility router for agents and tools that still start from `BUILDPRINT.md`.

Read `START_HERE.md`, then `blueprint.yaml`, then the active capability packet named by `02-context/context-map.yaml`.
Do not treat generated prompts or compatibility docs as source of truth.

