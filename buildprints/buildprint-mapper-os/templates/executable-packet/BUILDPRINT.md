# BUILDPRINT: <mapped-app>

This file is a compatibility router for agents and tools that still start from `BUILDPRINT.md`.

Read `START_HERE.md`, then `blueprint.yaml`, then the active context and capability packet. Do not treat `generated/agent-prompt.md` or `generated/current-buildprint-compat/` as source of truth.

