# BUILDPRINT: <mapped-app>

This is the package overview. The execution authority is `START_HERE.md` and `blueprint.yaml`.

Agents must read `START_HERE.md`, then `blueprint.yaml`, then the active capability named by the context map. Do not treat `generated/agent-prompt.md` as source of truth.
