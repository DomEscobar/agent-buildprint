# Phase-Driven Executable Buildprint Template

This is the Mapper OS v3 selected-packet template.

It is built around a generic AI-builder briefing, a concrete product-contract mirror, a mandatory UI/generated identity contract, and comprehensive phase documents that an AI builder can actually read and execute.

Start at `BUILDPRINT.md`, answer only real blockers in `00-questions.md`, generate UI identity from `01-ui-identity.md`, complete `02-project-setup.md`, then follow `03-phases/phase-flow.md` and the active phase file.
