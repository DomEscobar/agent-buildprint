# Product Name

![Version](https://img.shields.io/badge/version-0.1.0-blue)
![Build](https://img.shields.io/badge/build-local%20checks%20required-yellow)
![License](https://img.shields.io/badge/license-TBD-lightgrey)
![Runtime](https://img.shields.io/badge/runtime-TBD-lightgrey)
![Status](https://img.shields.io/badge/status-local%20build%20requires%20review-yellow)

One clear sentence explaining what this product does for its user or operator.

## Features

- Replace with the first feature users actually experience.
- Replace with the central workflow or output the product produces.
- Replace with the main editing, automation, runtime, or operator capability.
- Replace with the persistence, export, collaboration, provider, or integration capability when it exists.

## Requirements

- Runtime version only if the user/operator must install it manually.
- Package manager or CLI only if it is needed to run the product.
- Provider/API keys only for enabled live providers.
- Database, storage, export, webhook, OAuth, or deployment credentials only for enabled features.
- External service accounts only when the product actually calls that service.

## Environment And Provider Keys

Create a local `.env` from `.env.example`. Keep secrets out of documentation, logs, tests, screenshots, and handover notes.

```bash
REQUIRED_PROVIDER_API_KEY=
OPTIONAL_PROVIDER_API_KEY=
DATABASE_URL=
STORAGE_BUCKET=
```

- Required keys: list every key needed for the golden path or mark none.
- Optional keys: list providers that unlock live generation, export, sync, email, storage, billing, or deployment.
- Blocked proof: list any provider, sandbox, webhook secret, deployment token, or credential that was unavailable during verification.

## Quick Start

```bash
install-command
configure-command
run-command
test-or-check-command
```

Open the product through its real UI, CLI, API, or operator surface and complete the first useful action.

## Verification

- Build/check command: replace with the command and observed result.
- Test command: replace with the command and observed result.
- Manual surface proof: replace with the route, CLI action, API call, screenshot inspection, or runtime path used.
- Persistence/readback proof: replace with the observed reload/refetch/readback result when applicable.

## Limitations

- List missing live-provider proof, deployment proof, security hardening, browser proof, storage durability, or external-side-effect proof that remains blocked or unverified.
