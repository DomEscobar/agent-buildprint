# Runtime Topology

Describe runtime boundaries required by the selected capability: UI, API, worker/job, provider adapter, storage, events, and readback path. Mark absent boundaries as not applicable instead of omitting them silently.
