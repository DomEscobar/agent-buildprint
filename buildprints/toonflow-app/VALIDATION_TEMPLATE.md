# VALIDATION_TEMPLATE

## Scope

Validate the Toonflow App full-suite selected Buildprint from `BUILDPRINT.md`, `CAPABILITY_INDEX.md`, and every `capabilities/*` pack. Preserve selected scope unless Dom explicitly excludes a capability.

## Commands Run

Record exact commands here:

```bash
# install/check commands
# API/browser/runtime/security commands
```

## Evidence

Attach or reference proof artifacts for every capability proof-ledger row:

- static/build evidence:
- API evidence:
- browser/Electron evidence:
- provider/runtime evidence:
- persistence/restart evidence:
- security/privacy evidence:
- no-fake scan evidence:
- clean-room reversal evidence:

## Blockers / Gaps

Do not hide proof gaps. Keep capabilities at `CONTRACT_SEAM_ONLY` or `BLOCKED_WITH_REASON` until evidence exists.
