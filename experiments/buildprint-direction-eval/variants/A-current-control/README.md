# Variant A — Current Control

Use the current source packet at:

`buildprints/novel-storyboard`

Snapshot before running the agent. This is the proof/evidence-heavy control packet as of the experiment start.

Current source commit should be recorded in the run log with:

```bash
git rev-parse HEAD
git status --short
```

Do not modify this packet during the experiment run.
