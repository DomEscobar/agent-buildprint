# Human Review

Judge the product, not the process:

- Can a first-time operator complete the Microfish intake loop?
- Is the ontology useful enough to guide later simulation work?
- Are providers and runtime limits honest?
- Does state read back after reload?
- Does changed input change output?
- Is the UI clear and domain-specific, not generic dashboard filler?
