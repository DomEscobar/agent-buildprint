# Goal

A first-time user can run Microfish locally, create an intake project from seed evidence, generate a deterministic ontology/graph preview, inspect and edit/read back the result, reload without losing it, and export JSON — with no fake provider/runtime success claims.
