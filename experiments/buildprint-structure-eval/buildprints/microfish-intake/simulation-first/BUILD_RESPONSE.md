# Build Response

Implement the smallest local product that removes every open failure in `FAILURE_MAP.md`.

Required surfaces:

- local startable app;
- project/evidence input;
- ontology generation from seed notes;
- ontology review panel;
- provider status panel;
- local persistence/readback;
- export JSON;
- tests and user-smoke script if feasible.

Forbidden response:

- dead controls;
- fake provider success;
- hardcoded ontology unrelated to input;
- only writing proof docs instead of building product.
