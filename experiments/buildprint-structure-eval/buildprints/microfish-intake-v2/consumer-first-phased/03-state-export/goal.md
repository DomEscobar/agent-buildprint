# Phase 03 — State + Export

Goal: user can leave and return without losing the analysis, then export it.

Acceptance:

- local persistence/readback works;
- visible saved/restored state;
- export JSON includes project/name, notes, plain-language result, structured ontology, provider status, timestamp;
- export button is not dead.
