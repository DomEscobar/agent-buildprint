# Phase 01 — Architecture Foundation

Create the app skeleton before feature coding.

Required modules:

- domain analysis module;
- provider status module;
- project storage module;
- export payload module;
- UI shell/components;
- tests for pure modules.

Acceptance: a future agent can find where to add graph memory or simulation prep without reading a monolith.
