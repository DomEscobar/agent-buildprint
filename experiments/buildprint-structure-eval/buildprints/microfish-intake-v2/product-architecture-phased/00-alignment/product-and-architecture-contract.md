# Product + Architecture Contract

Product promise:

A normal user can paste notes and receive a simple analysis of people, topics, risks, and next steps.

Architecture promise:

The code must be ready for future Microfish phases: graph memory, simulation prep, runtime, reports, and history.

Required source shape:

```text
src/
  app/
  components/
  domain/
  storage/
  provider/
  export/
  styles/
  tests or test/
```
