# Stack And Scope

Default stack:

- Vite + TypeScript + React or Svelte.
- Modular source: `domain`, `storage`, `provider`, `components`, `export`.

Scope:

- Microfish intake + simple analysis only.
- No real provider calls.
- No production deployment.
- Product must be usable by a non-technical first-time user.
